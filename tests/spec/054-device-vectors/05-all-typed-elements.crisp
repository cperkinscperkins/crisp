;; TEST-EXPECT: PASS

;; Verifies that ##(...) with all elements carrying the same type suffix
;; produces the correct device vector type.

(def-function make-float4 ()
  (declare (return-type float4))
  ##(1.5f 2.5f 3.5f 4.5f))
