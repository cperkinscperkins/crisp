;;
;; CHECK-FAIL: "w~ is out of range for FLOAT2 (width 2);"
;;
;; w~ on a width-2 vector (only x, y exist) should be a type error.

(def-function bad-range (v)
  (declare #'(float2 => float))
  (w~ v))
