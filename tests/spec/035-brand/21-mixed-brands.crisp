;; TEST-EXPECT: PASS 
;; FAIL-WITH[--differentiate]: "message"

(def-record server
  ;; custom types
  (brand token-t ulong :subst :equal)
  (brand id-t ulong :subst :equal)
  
  ;; members
  (active-token token-t)
  (id id-t))


(def-function super-authenticate (s tok id)
  (declare #'(server (token-t s) (id-t s) => int))
  
  (+ tok id)) ;; passes normally, errors when brands are enforced.