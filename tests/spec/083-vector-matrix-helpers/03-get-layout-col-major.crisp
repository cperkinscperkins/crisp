;; TEST-EXPECT: PASS
;; TEST-HOIST[L0]: validate-l0-host-run
;; TEST-HOIST[CUDA]: validate-cuda-host-run
;; HOIST-EXPECT: BUFFER out: 1
;; get-layout on a :major :col matrix returns :col-major.
;; Col-major: strides=[1,3] for a 3x4 matrix (stride_col = num_rows = 3).

(def-type int-vec-t (vector int :align :compact :address-space :global))
(def-type out-cell-t (cell int :address-space :global))

(def-kernel test_get_layout_col (v &out out)
  (declare #'(int-vec-t &out out-cell-t => nil))
  (let ((M (make-matrix v int 4 3 :major :col)))
    (set! (~ out) (if (= (get-layout M) :col-major) 1 0))))
