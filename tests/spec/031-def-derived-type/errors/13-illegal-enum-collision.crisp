;; CHECK-FAIL: "Cannot define derived type COLORS: name collides with existing type/struct/enum."

(def-enumeration colors :red :blue :brown)

(def-derived-type colors float :subst :descendant)

#|
(def-function what-is-arg (c)
    (declare #'(colors => colors))
    (as-float 1.0))

|#

(def-function what-is-arg (c)
    (declare #'(colors => colors))
    c)