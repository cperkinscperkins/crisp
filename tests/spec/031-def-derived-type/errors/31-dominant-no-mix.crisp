;;
;; CHECK-FAIL: "Type mismatch for operator '+'. Cannot operate on METERS and YARDS"

(def-derived-type meters float :subst :ancestor)

(def-derived-type yards float :subst :ancestor)

(def-function miscegeny (m y)
    (declare #'(meters yards => float))
    ;; nope. that won't work. compile error.
    (+ m y))