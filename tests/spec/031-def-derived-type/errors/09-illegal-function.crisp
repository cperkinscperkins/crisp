
;; CHECK-FAIL: "Cannot derive type ADD-FLOATS from ADD-TWO: original type does not exist"

(def-function add-two (a b)
  (declare #'(float float => float))
  (+ a b))

(def-derived-type add-floats add-two :subst :equal) ;; error. 