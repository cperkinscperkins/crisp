;; CHECK-FAIL: "incomplete storage handle type"
;; (vector int) is missing :address-space and is therefore incomplete.
;; Kernels require fully specified types.
(def-kernel bad_kernel (v)
    (declare #'((vector int) => nil))
    (return))
