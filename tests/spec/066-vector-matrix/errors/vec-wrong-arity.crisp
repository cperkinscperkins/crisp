;; CHECK-FAIL: "Invalid type option 3 in vector spec"
;; vector takes no arity argument. Use (tensor T N) for arity > 2.
(def-function test-vector-wrong-arity (v)
    (declare #'((vector int 3) => nil))
    (return))
