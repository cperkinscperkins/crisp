;; TEST-EXPECT: PASS
;;
;; Endeavor 130 Phase 0: a PARTIAL profile is valid — missing keys are fine.
(def-hardware-profile tiny :simd-width 16)
(def-type out-c (cell float :address-space :global))
(def-kernel k (&out res) (declare #'(&out out-c => nil)) (set! (~ res) 0.0))
