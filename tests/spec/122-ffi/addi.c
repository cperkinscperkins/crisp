// Endeavor 122 (FFI) Pass 1, on-metal: an integer device function bound via
// def-foreign-function and exercised on real hardware through the hoist path.
int my_addi(int a, int b) { return a + b; }
