;; SKIP-WITH[--differentiate]: "kernel has no &out param, not differentiable"


(def-kernel simple_scalar (x)
    (declare #'(int => nil))
    ;; nothing to do really. One way trip to nowhere.
    (set! x (+ x 1)))