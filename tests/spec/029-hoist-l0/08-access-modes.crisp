

;; TEST-HOIST[L0] : validate-l0-cell-access-modes

(def-kernel access_test (ro rw &out wo)
  (declare (type ro (cell int :access :read-only))
           (type wo (cell int :access :write-only))
           (type rw (cell int :access :read-write)))
  (set! (~ wo) (+ (~ ro) (~ rw))))

