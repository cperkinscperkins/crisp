;; CHECK-FAIL: "Grid-level context cannot appear in a thread-level function"
;; loop-vector-stride requires a dispatch context (def-kernel / def-grid-function).
;; Using it inside a plain def-function must error.

(def-type vec-t (vector int :address-space :global :align :compact))

(def-function bad_stride (v)
  (declare #'(vec-t => nil))
  (loop-vector-stride v (i)
    (set! (~ v i) (to-int i))))
