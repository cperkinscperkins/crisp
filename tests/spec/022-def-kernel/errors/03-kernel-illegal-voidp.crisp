;; TEST-EXPECT: PASS

;; TODO: implement this error. This test should fail.

;; "not unreasonable error message"
;; 
;; SKIP-WITH[--differentiate]: "kernel has no &out param, not differentiable"

;; only def-kernel-exact can take voidp arguments.  def-kernel cannot, and will need 
;; custom screening for them

(def-kernel illegal_voidp (vp)
    (declare #'(voidp => nil)) ;; <-- nyet
    (return))