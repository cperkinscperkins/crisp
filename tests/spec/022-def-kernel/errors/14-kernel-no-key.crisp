;;
;; CHECK-FAIL: "Kernels (def-kernel/def-kernel-exact) do not support &optional or &key parameters."
;; 

;; neither def-kernel or def-kernel-exact support &optional or &key

(def-kernel key_unsupported (a &key b)
    (declare #'(int &key b int => nil))
    nil)