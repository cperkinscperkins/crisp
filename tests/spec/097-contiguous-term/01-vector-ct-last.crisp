;; TEST-EXPECT: PASS
;; TEST-WITH[--ir-target=spv]
;; vector declared with explicit :contiguous-term :last should compile fine.

(def-type vec-t (vector float :align :compact :address-space :global :access :read-write :contiguous-term :last))

(def-kernel test_vector_ct_last (v)
  (declare (type v vec-t))
  (return))
