
;; CHECK-FAIL: "set-derived: ancestor VERTEX (size 12 bytes) is larger than descendant POINT (size 8 bytes). The ancestor must be smaller or equal."

(def-struct point (x int) (y int))

(def-struct vertex (x int) (y int) (z int))

(set-derived vertex point) ;; 
