
;; CHECK-FAIL: "set-derived: ancestor type POINT is a record. set-derived only works with structs."

(def-record point (x int) (y int))

(def-struct coordinate (x int) (y int))

(set-derived point coordinate) ;; error. can't have record either arg