
;; CHECK-FAIL: "set-derived: shape mismatch at member index 1. Ancestor POINT has type INT but descendant VERTEX has type FLOAT. Types must match exactly"

(def-struct point (x int) (y int))

(def-struct vertex (x int) (y float)) ;; float is same size as int. But still disallowed.

(set-derived point vertex)