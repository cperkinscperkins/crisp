
;; CHECK-FAIL: "set-derived: descendant type POINT is a record. set-derived only works with structs"

(def-record point (x int) (y int))

(def-struct coordinate (x int) (y int))

(set-derived coordinate point) ;; error. can't have record as either arg