;; CHECK-FAIL: "set-derived: shape mismatch at member index 1. Ancestor POINT has type INT but descendant VERTEX has type UINT. Types must match exactly"

(def-struct point (x int) (y int))

(def-struct vertex (x int) (y uint)) ;; Still disallowed.

(set-derived point vertex)