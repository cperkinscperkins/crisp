;; CHECK-FAIL: "set-derived: ancestor type INT is not a struct. Only structs (or types derived from structs) are permitted"


(def-struct coordinate (x int) (y int))

(set-derived int coordinate) 