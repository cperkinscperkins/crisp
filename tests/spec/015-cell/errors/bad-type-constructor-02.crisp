
;; missing keyword
;; CHECK-FAIL: "Invalid incomplete type specifier: (CELL LONG GLOBAL)."
(def-function cell-missing-keyword (c)
    (declare #'((cell long :global) => ulong)) ;; <-- (cell long :address-space :global) would be correct
    (byte-size~ (parent~ c)))