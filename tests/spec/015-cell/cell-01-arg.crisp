;; cell is a parameterized type. 
(def-function test-cell-as-arg (c1 c2 c3)
    (declare #'(cell (cell) (cell long) => nil))
    (return))

(with-template-type (T)
    (def-function test-templated-cell-as-arg (c)
        (declare #'((cell T) => nil))
        (return)))

(gen-test-templated-cell-as-arg float)