;; CHECK-FAIL: "requires keyword arguments"
(def-struct point 
    (x long) 
    (y long))

;; this should fail.
(def-function make-attempt (x y)
    (declare #'(long long => point))
    (make-point  x  y))