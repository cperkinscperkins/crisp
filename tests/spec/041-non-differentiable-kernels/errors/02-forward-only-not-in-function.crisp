
;; Should this fail ONLY when --differentiate is used?

;; forward-only is only allowed in def-kernel and def-grid-function.

(def-function dont-know-what-i-am-doing (C) 
  (declare forward-only #'((cell float :address-space :global :access :read-write) => nil))
  (set! (~ C) 2.0))