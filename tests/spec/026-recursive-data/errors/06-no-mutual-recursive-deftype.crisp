;; CHECK-FAIL: "An unexpected error occurred: Recursive type alias detected: Type alias 'QUX' refers to itself directly or indirectly."

;; mutual recursive (invalid)
(def-type qux zum)
(def-type zum qux)

;; once again, infinite loop hang when compiling
(def-function qux-user (q)
    (declare #'(qux => qux))
    (+ q q))