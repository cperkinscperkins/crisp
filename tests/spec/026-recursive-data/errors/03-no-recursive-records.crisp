;;
;; CHECK-FAIL: "Could not resolve all struct definitions. cycles or unknown types detected"
;;

;; records can contain other records, but cannot be recursive

(def-record virtual-point (x int) (y int) (vp virtual-point))