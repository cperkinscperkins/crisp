;;
;; CHECK-FAIL: "Could not resolve all struct definitions. cycles or unknown types detected"
;;

;; structs cannot be mutually recursive

(def-struct pointish (x int) (y int) (pt pointie))

(def-struct pointie (xx int) (yy int) (pi pointish))

