;; CHECK-FAIL: "Type mismatch! Expected (INT) but inferred (KEYWORD FOO)."
;; This function declares an int return type but returns a keyword.
(def-function unsupported-keyword ()
  (declare (return-type int)) :foo)

