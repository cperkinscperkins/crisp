
;; CHECK-FAIL: "Kernels cannot declare they return values."

;; kernels cannot declare they return values 
(def-kernel-exact too_fancy (a b)
    (declare #'(long long => long))
    (+ a b)
    (return nil))