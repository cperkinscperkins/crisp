;; TEST-WITH[--ir-target=llvmir]

;; this is for testing llvmir generation
;; SKIP-WITH[--differentiate]: "kernel has no &out param, not differentiable"

(def-kernel smoke_test (c)
  (declare #'((cell int :address-space :global)))
  (set! (~ c) 42))
