;; TEST-WITH[--ir-target=llvmir]

;; this is for testing llvmir generation
;; SKIP-WITH[--differentiate]: "fails without lax mode"
(def-kernel smoke_test (c)
  (declare #'((cell int :address-space :global :access :read-write)))
  (set! (~ c) 42))
